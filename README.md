# cowry-cli

## Install.

```bash
  npm i -g cowry-cli

  // or
  yarn add -g cowry-cli

  // or
  npx cowry-cli
```

## Usage.

- 初始化项目

```bash
  cowry init [template-name] [folder-name]
```

- 自定义模版路径

```bash
  cowry init -s [template-url] [folder-name]
```
